{
  "name": "design-mining-responsive-web-pages",
  "version": "0.0.1",
  "description": "https://playwright.dev/docs/docker",
  "main": "index.js",
  "scripts": {
    "build": "docker build -t design-mining .",
    "dev": "docker run -it --rm --ipc=host --user pwuser --security-opt seccomp=seccomp_profile.json --mount type=bind,src=\"$(pwd)\",target=/app design-mining ts-node index.ts",
    "start": "docker run -it --rm --ipc=host --user pwuser --security-opt seccomp=seccomp_profile.json design-mining /bin/bash"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/node": "18.11.18",
    "ts-node": "10.9.1",
    "typescript": "4.9.5"
  }
}
